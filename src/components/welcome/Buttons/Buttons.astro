<article>
  <img src="../../../public/img/icon-help.svg" alt="">
  <p class="regular-text"><span class="regular-text--bold">99</span> algo mais</p>
</article>
<article>
  <img src="../../../public/img/icon-help.svg" alt="">
  <p class="regular-text"><span class="regular-text--bold">99</span> algo mais</p>
</article>
<button class="regular-text regular-text--3 regular-text--bold">Agendar consulta</button>

<style>

article > img{
  width: var(--fs-5);
  height: var(--fs-5);
  margin-inline: auto;
  margin-bottom: .5rem;
}

button{
  display: flex;
  justify-content:center;
  align-items: center;
  gap: .5rem;

  border: none;
  
  background: none;
  
  grid-column: span 2;
}

article, 
button{
  position: relative;
  
  padding: .75rem;
  border: 2px solid currentColor;

  background-color: var(--light-green-200);

  color: var(--dark-green-200);
  text-align: center;
}

article::after,
button::after{
  content: '';

  position: absolute;
  inset: 0;
  bottom: -3px;
  right: -3px;
  z-index: -1;

  background-color: var(--dark-green-200);
}

button::before{
  content: '';

  display: inline-block;

  width: var(--fs-5);
  height: var(--fs-5);
  
  background-image: url(../../../public/img/icon-calendar.svg);
  background-repeat:no-repeat;
  background-size: 100% 100%;
  background-position:center;
}

  /* A partir de 800px */
  @media (min-width: 50em){
    article{
      display: flex;
      align-items: center;
      justify-content: center;
      gap: .5rem;
    }

    article:nth-of-type(1){
      grid-column: 1/2;
    }

    article:nth-of-type(2){
      grid-column: 2/3;
    }

    article > img{
      margin-bottom: 0;
      margin-inline: 0;
    }

    button{
      grid-row: 1/2;
      grid-column: span 1 / -1;
    }

    article, 
    button{
      padding: .75em;
    }
    
    article::after,
    button::after{
      bottom: -5px;
      right: -5px;
    }

    button::before{
      width: 1em;
      height: 1em;
    }
  }

</style>